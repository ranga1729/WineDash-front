import { CSSProperties } from "react"

const MainContent = () => {
  return (
    <div style={Main}>

    </div>
  )
}

export default MainContent

const Main:CSSProperties = {
  display: 'flex',
  flex: '1',
  minWidth: '500px'
}